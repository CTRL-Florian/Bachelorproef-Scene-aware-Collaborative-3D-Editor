import TitleBar from "@/header/TitleBar";
import WorkPanel from "@/header/WorkPanel";

function Header() {
    return (
        <>
            <TitleBar />
            <WorkPanel />
        </>
    );
}

export default Header;